// Interface in TypeScript is a powerful way to define the structure of an object. It allows you to specify the properties and their types that an object should have. This can help catch errors at compile time and improve code readability.
// interface User{
//     name: string;
//     age: number;
//     email: string;
// }

// function Users(user: User){
//     console.log(`Name: ${user.name}, Age: ${user.age}, Email: ${user.email}`);
// }


import { PI } from "./modules";

console.log(`Value of PI: ${PI}`);




// Type Assertions - Type assertions in TypeScript allow you to tell the compiler about the type of a variable when you have more information than it does. This can be useful when you know the type of a variable but the compiler cannot infer it.

// Literal Type - Literal types allow you to specify that a variable or parameter can only have a specific set of values. This can be useful for creating more precise types and improving type safety.

// Union Types - Union types allow you to specify that a variable or parameter can be one of several types. This can be useful for handling cases where a value can be of different types and improving type safety.

// Type Aliases - Type aliases allow you to create a new name for a type. This can be useful for improving code readability and creating more complex types.
 